# Complexity Delta

Keep one analysis path: read snapshots, analyze, compare, render. Use TypeScript’s resolver and AST knowledge instead of duplicating them. Treat metrics as evidence, not scores.

Run `pnpm test` and `pnpm typecheck` after code changes. Once behavior is correct, follow [the subtraction skill](skills/complexity-delta/SKILL.md). For this repository, run `node dist/cli.js HEAD`.
